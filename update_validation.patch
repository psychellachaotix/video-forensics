--- artifacts/video-forensics/src/pages/home.tsx
+++ artifacts/video-forensics/src/pages/home.tsx
@@ -52,7 +52,7 @@
   const addFiles = (files: File[]) => {
     const accepted: File[] = [];
     for (const file of files) {
-      if (!file.type.startsWith('video/') && !/\.(mp4|mov|mkv|avi|webm|m4v)$/i.test(file.name)) {
+      if (!/\.(mp4|mov|mkv|avi)$/i.test(file.name)) {
         toast({ title: t.unsupportedFormat, description: t.onlyMp4MovAviMkv, variant: 'destructive' });
         continue;
       }
